# Owner Command Centre progress

Date: 2026-09-10 (Europe/London)

## Scope this pass
Job detail / ManagerActions / GroupDispatch / Home / Jobs / Login / Money (read-only check) / atelier.css.
**Inbox:** ConversationsInbox left untouched — sibling agent owns inbox photo/media rendering. Do not edit further here.

## File-by-file

### `app/jobs/[id]/ManagerActions.tsx`
- `offers_received` now treated as assignment-eligible (offer assign via `/api/fitter-assignment` when URL configured).
- Manual/guest assign includes `offers_received`.
- When `awaiting_group_dispatch`, duplicate Group Message copy is suppressed — GroupDispatchCard owns copy + live offers; manual “I Found a Fitter” remains.
- Honest first-refusal / assignment not-configured notices + WhatsApp/Inbox/Call fallbacks unchanged.

### `app/jobs/[id]/page.tsx`
- Hero next-step for `awaiting_payment` / `payment_link_expired` points to `#payment-resend` (panel owns Rescue-red primary).
- `PaymentResendActions` renders for expired **and** plain `awaiting_payment` (WhatsApp primary; webhook only if `TYREOPS_PAYMENT_RESEND_URL` set — never invent success).
- Settlement chase + Yes/No (`1c368d9`) kept.
- Contact row: Call / WhatsApp / Maps / Inbox / Take over — unchanged, not duplicated in hero.

### `app/jobs/[id]/GroupDispatchCard.tsx`
- On status error / not-configured: Inbox + Retry fallbacks (no fake success).

### `app/jobs/[id]/PaymentResendActions.tsx`
- Unchanged this pass (already WhatsApp-primary + optional webhook).

### `app/page.tsx` (Home)
- Needs You action verbs: group dispatch, chase payment, decide now, assign fitter.
- Earnings strip remains whisper; Needs You dominates (CSS nudge).

### `app/jobs/page.tsx`
- CTA verbs aligned; `awaiting_payment` treated as actionable for primary-card red.

### `app/login/page.tsx`
- Fixed mojibake ellipsis on sign-in / reset / success copy (en-GB).

### `app/atelier.css`
- `groupDispatchOwnsCopy` quiet note; earnings strip whisper; Needs You depth.

### `app/conversations/ConversationsInbox.tsx`
- **Not edited** (sibling owns inbox media).

### `app/money/page.tsx`
- No code change this pass — chase WhatsApp + settlement links already present.

## Status → button matrix

| Status | Working buttons | WhatsApp / Inbox / Call fallback | Still gapped (backend) |
|---|---|---|---|
| `awaiting_owner_price` | Confirm & send quote → `/api/price-job` | — | Needs control token + deposit rules |
| `awaiting_payment` / expired | Chase WhatsApp + Inbox; optional webhook resend | Primary path | Safe `TYREOPS_PAYMENT_RESEND_URL` (do not point at tyreops-api/resend-payment) |
| `awaiting_owner_first_refusal` | Take / Decline / Snooze when URL set | Inbox / WA / Call when URL missing | `TYREOPS_FIRST_REFUSAL_URL` |
| `awaiting_group_dispatch` | Copy + assign offers (`/api/group-dispatch`); manual assign | Inbox + Retry on error | `TYREOPS_GROUP_DISPATCH_URL` |
| `offers_received` / `awaiting_owner_assignment` | Assign offer + manual/guest | Inbox / WA / Call if assignment URL missing | `TYREOPS_FITTER_ASSIGNMENT_URL` |
| `deposit_paid` / dispatching_* / manual_review | Manual/guest assign + group copy | — | Manual needs `TYREOPS_CONTROL_TOKEN` |
| In-progress assigned | Call / WA / Maps / Inbox / Take over | — | — |
| `completed` | Chase fitter balance WA + Yes/No settlement | Open settlement if no phone | — |

## Remaining backend gaps only
1. `TYREOPS_FIRST_REFUSAL_URL` (+ control token)
2. `TYREOPS_FITTER_ASSIGNMENT_URL`
3. `TYREOPS_GROUP_DISPATCH_URL`
4. Safe `TYREOPS_PAYMENT_RESEND_URL` (when ready — **not** tyreops-api/resend-payment)
5. Automation switches stay false — not touched

## Commits / verify
See git log after this pass. `npx tsc --noEmit` + `npm run build` + preview restart expected.
