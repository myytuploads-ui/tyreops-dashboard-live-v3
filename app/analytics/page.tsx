import { createClient } from '@/lib/supabase/server';
import PageHeader from '@/components/PageHeader';

export default async function AnalyticsPage(){const s=await createClient();const [{data:jobs},{data:events}]=await Promise.all([s.from('jobs').select('status,customer_price,agreed_fitter_cost,created_at,completed_at').limit(1000),s.from('workflow_events').select('event_type,created_at').order('created_at',{ascending:false}).limit(100)]);const completed=(jobs||[]).filter(j=>j.status==='completed');const avg=(completed.length?completed.reduce((n,j)=>n+(Number(j.customer_price)||0)-(Number(j.agreed_fitter_cost)||0),0)/completed.length:0);return <><PageHeader title="Analytics" subtitle="Operational signals from authoritative job history."/><div className="ownerMetricGrid"><div className="metricCard"><div className="metricLabel">Jobs tracked</div><div className="metric">{(jobs||[]).length}</div><div className="metricNote">Live and historical</div></div><div className="metricCard"><div className="metricLabel">Completed</div><div className="metric">{completed.length}</div><div className="metricNote">Recorded completions</div></div><div className="metricCard"><div className="metricLabel">Average margin</div><div className="metric kpiAccent">£{Math.round(avg)}</div><div className="metricNote">Known completed costs</div></div><div className="metricCard"><div className="metricLabel">Recent events</div><div className="metric">{(events||[]).length}</div><div className="metricNote">Latest operational pulse</div></div></div><section className="panel"><div className="panelHead"><h2>Insights</h2><p>Recommendations are informational only; nothing changes automatically.</p></div><div className="panelBody"><div className="readonly">INSIGHT · Review quote delays, fitter ETA accuracy and repeat owner interventions from completed jobs before changing any rules.</div></div></section></>}
